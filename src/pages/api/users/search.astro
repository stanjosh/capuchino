---
import CustomerTable from '../../admin/partials/CustomerTable.astro'

import db from '../../../../lib/db'

const formData = await Astro.request.formData()

const customers = await db.user.search( formData.get('search') );

---
<CustomerTable data={customers} />

